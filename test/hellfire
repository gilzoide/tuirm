cpp {
	input = utils.glob '*.cpp',
	includes = '../include',
	flags = '&-g',
	links = 'tuirm',
	libDirs = '../' .. hell.outdir,
	std = 'c++11',
	output = 'main',
	deps = {libtuirm}
}

exclusiveTarget ('run', command {
	command = 'LD_LIBRARY_PATH=' .. hell.outdir .. ' build/main',
	args = arg
})
